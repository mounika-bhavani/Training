#include<stdio.h>
int int main()
{
	int *marks;
	int noOfMarks;

	printf("Enter number of marks: \n");
	scanf("%d", &noOfMarks);
	
	marks = (int*)malloc(noOfMarks * sizeof(int));
	int marks[noOfMarks];

	for (int counter = 0; counter < noOfMarks; counter++f)
	{
		/* code */
	}
	return 0;

}